<h1 align="center">🌱 Семя AntHub</h1>

<p align="center">Один репозиторий — один сервер и его клиентская сборка.</p>

Игрок добавляет ссылку на ваш GitHub-репозиторий в AntHub и устанавливает сборку. Сайт и ключи публикации не нужны.

## 1. Скопируйте шаблон

Перенесите **всё содержимое** этой папки в корень публичного GitHub-репозитория, включая скрытую `.github`. После fork включите workflows во вкладке **Actions**, если GitHub попросит.

```text
anthub.json          Название, адрес, версия и компоненты
files/               Конфиги и другие файлы сборки
content/rules.md     Правила сервера
content/news/        Новости в Markdown
.github/             Проверки и публикация
schemas/             Схемы проверки JSON
tooling/             Скрипты публикации
```

`schemas`, `tooling` и `.github` уже входят в шаблон: копируйте и обновляйте их вместе, редактировать их для обычной сборки не нужно.

## 2. Опишите сервер

Откройте `anthub.json` и заполните:

| Поле                      | Что указать                                                         |
| :------------------------ | :------------------------------------------------------------------ |
| `name`                    | Название в AntHub и списке серверов Minecraft.                      |
| `version`                 | Версия сборки, например `1.0.0`.                                    |
| `server.address`          | Домен или IP, при необходимости с портом: `play.example.org:25565`. |
| `minecraft.version`       | Версия Minecraft.                                                   |
| `minecraft.loaderVersion` | Версия NeoForge, на которой проверена сборка.                       |
| `components`              | Моды и файлы для установки — примеры ниже.                          |

Описание MOTD, иконку, онлайн и задержку AntHub получает с Minecraft-сервера. Дублировать их не нужно. Minecraft и NeoForge игрок устанавливает через свой лаунчер.

## 3. Добавьте компоненты

Каждый элемент `components` — мод или группа файлов. В шаблоне есть учебный конфиг: замените его своим компонентом и файлом либо удалите оба.

### Мод по ссылке

Добавьте в массив `components` такой объект, заменив название и ссылку:

```json
{
  "id": "example-mod",
  "name": "Название мода",
  "description": "Для чего он нужен игроку.",
  "kind": "optional",
  "files": [
    {
      "path": "mods/example-mod.jar",
      "urls": ["https://example.org/downloads/example-mod.jar"]
    }
  ]
}
```

Ссылка должна вести **непосредственно на файл** нужной версии, а не на страницу мода. Адрес из примера замените своим.

### Конфиг из репозитория

Положите файл в `files/your-mod-client.toml` и добавьте компонент:

```json
{
  "id": "client-config",
  "name": "Настройки сборки",
  "kind": "required",
  "files": [
    {
      "path": "config/your-mod-client.toml",
      "repositoryPath": "files/your-mod-client.toml",
      "policy": "preserve"
    }
  ]
}
```

`path` — место установки в игровой папке. `repositoryPath` — расположение исходного файла в вашем репозитории.

### Обязательность и зависимости

- `required` — обязательный компонент; используется, если `kind` пропущен.
- `recommended` и `optional` — включены по умолчанию, но игрок может снять галочку. Его выбор сохраняется.
- `id` — постоянное уникальное имя из строчных латинских букв, цифр и дефисов.
- `dependencies: ["library-id"]` — компоненты, необходимые этому моду.
- `conflicts: ["other-id"]` — несовместимые компоненты.

### Запасные ссылки

В `urls` можно указать до **8 прямых HTTPS-ссылок** в порядке предпочтения. AntHub сначала проверяет кэш, затем пробует источники по очереди.

**Размер и SHA-256 вычисляются автоматически.** Все зеркала должны отдавать одинаковые байты: одинаковое название и версия мода на разных площадках этого не гарантируют. Недоступное зеркало вызывает предупреждение при публикации, несовпадение файлов — ошибку. Если недоступны все источники, выпуск не создаётся.

Для `repositoryPath` эталоном служит ваш файл; его копия в GitHub Release становится первым источником. При необходимости можно добавить `urls` с зеркалами той же копии.

### Обновление файлов

| `policy`   | Поведение                                                                             |
| :--------- | :------------------------------------------------------------------------------------ |
| `enforce`  | Привести файл к версии сборки. Обязательно для `mods/`.                               |
| `preserve` | Сохранить пользовательские изменения. По умолчанию для `config/` и `defaultconfigs/`. |
| `update`   | Обновить, если игрок не менял файл. По умолчанию для остальных разрешённых папок.     |

Не включайте миры, `options.txt`, `servers.dat`, пароли и внутренние файлы AntHub. Здесь размещаются только **клиентские файлы**; серверные моды устанавливаются отдельно.

## 4. Напишите правила и новости

- Правила — `content/rules.md`.
- Новости — отдельные `.md` в `content/news/`, например `2026-09-16-update.md`.

Замените учебные тексты своими. Ненужные файлы можно удалить.

## 5. Опубликуйте сборку

1. Сделайте commit и push в основную ветку.
2. Во вкладке **Actions** дождитесь успешного **Publish pack**.
3. В **Releases** появится `pack-v1.0.0`, а в репозитории — `channels/stable.json`.
4. Добавьте ссылку `https://github.com/ВАШ_АККАУНТ/ВАШ_РЕПОЗИТОРИЙ` в AntHub и проверьте установку.

Первый выпуск создаётся автоматически. Для следующих повышайте `version`: `1.0.0` → `1.0.1`. Старые номера не переиспользуйте. `anthub.lock.json` и `channels/stable.json` создаются автоматически — вручную их заполнять не нужно.

GitHub выдаёт токен публикации автоматически. Если правила репозитория ограничивают запись из Actions, разрешите workflow публиковать изменения `channels/`.

## 🔄 Что требует новой версии

| Изменение                                      | Что сделать                                                                  |
| :--------------------------------------------- | :--------------------------------------------------------------------------- |
| Название или адрес сервера                     | Изменить `name` / `server.address` и сделать push. Версию повышать не нужно. |
| Моды, конфиги, требования, новости или правила | Повысить `version` и сделать push.                                           |

Название и адрес проверяются при запуске игры, перед подключением и вручную. Перед подключением успешная проверка младше минуты используется повторно. При недоступной сети остаются последние корректные данные. Периодических запросов к GitHub нет; онлайн и MOTD обновляются напрямую с сервера.

## 🛠️ Если публикация не прошла

Откройте упавший шаг в **Actions**. Проверьте прямые ссылки, совместимость модов с Minecraft/NeoForge и новую версию сборки. После устранения причины повторите запуск через **Re-run failed jobs** или отправьте исправления.

Проверка читает метаданные JAR и не запускает моды. Успешная публикация не заменяет пробный запуск всей сборки.

<details>
<summary>Дополнительные настройки и локальная проверка</summary>

- `sha256` у файла — необязательное закрепление конкретного содержимого. При несовпадении публикация остановится.
- `version` у файла — необязательна; по умолчанию используется версия сборки.
- `policies.customFiles` — `warn` по умолчанию или `deny`.
- `anthub.minVersion`, `integrations.luckperms` и `theme` — дополнительные требования и оформление.

Для локальной проверки:

```sh
python -m pip install -r tooling/requirements.txt
python tooling/anthub.py validate .
```

Проверка модов учитывает метаданные NeoForge и его правила совместимости. Неизвестные диапазоны версий выводятся как предупреждения. Совместимость кода модов и вложенных зависимостей нужно проверять запуском игры.

</details>

---

<p align="center">🐜 Семя готово — поделитесь ссылкой с игроками.</p>
